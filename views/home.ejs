<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Homepage</title>
</head>
<body>
     <h1>Url shortener</h1>
     <% if (locals.id) {  %> 
        <p> URL Generated : https://localhost:8001/shortened/<%= id %> </p> 
     <% } %> 
     <div>
        <form method="POST" action="/url">
            <label>
                Enter you original URL!
            </label>
            <input type="text"  placeholder="www.example.com" name="url">
         <button type="submit">Generate</button>
        </form>
     </div>

     <table border="1">
        <thead>
            <tr>
                <th>Sno</th>
                <th>Short ID</th>
                <th>Redirect ID</th>
                <th>Number of Clicks</th>
            </tr>
        </thead>
        <tbody>
            <!-- Add rows dynamically based on your data -->
            <% /* Assuming 'urls' is an array containing your URL records */ %>
            <% for (let i = 0; i < urls.length; i++) { %>
                <tr>
                    <td><%= i + 1 %></td>
                    <td><%= urls[i].shortId %></td>
                    <td><%= urls[i].redirectUrl %></td>
                    <td><%= urls[i].visitHistory.length %></td> <!-- Adjust this based on your data structure -->
                </tr>
            <% } %>
        </tbody>
    </table>
</body>
</html>
